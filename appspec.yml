version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/codedeployscripts

hooks:
  BeforeInstall:
    - location: scripts/install_dependencies.sh
      timeout: 500
      runas: root
