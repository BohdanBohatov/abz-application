version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/codedeployscripts

hooks:
  BeforeInstall:
    - location: /home/ec2-user/codedeployscripts/scripts/configure_wp.sh
      timeout: 500
      runas: root
